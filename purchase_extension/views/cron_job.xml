<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="ir_cron_purchase_order_recurring_tasks" model="ir.cron">
        <field name="name">purchase Order Recurring Tasks</field>
        <field name="model_id" ref="model_purchase_order"/>
        <field name="state">code</field>
        <field name="code">model.recurring_purchase_order()</field>
<!--        <field name="user_id" ref="base.user_root" />-->
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="numbercall">-1</field>
<!--        <field name="nextcall" eval="(DateTime.now() + timedelta(minutes=1)).strftime('%Y-%m-%d %H:%M:%S')"/>-->
        <field name="doall" eval="False"/>
        <field name="priority">5</field>
    </record>

</odoo>